# Folder Fixture Note

This file lives inside `sample-folder` for selected-folder listing and bounded read tests.

Expected behavior:

- Folder listing should show immediate children.
- Folder content reading should occur only when the user explicitly asks to read files.
