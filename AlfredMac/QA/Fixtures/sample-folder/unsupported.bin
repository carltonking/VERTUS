This file intentionally uses an unsupported extension.
It is plain text content with a .bin extension so it is safe in the repository.
Alfred should list it, but should not read it as supported selected-folder content.
