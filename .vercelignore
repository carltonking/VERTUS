# Alfred Lite (Vercel) only needs api/ + website/ + the root configs. Exclude the giant macOS Swift
# app and build artifacts so the upload stays under Vercel's file limit.
AlfredMac
.build
build
node_modules
Tune
Logos
archive
Fine Tune
dist
.github
.vscode
.claude
*.md
com.alfred.launcher.plist
install.sh
install_launchd.sh
mcp_config.example.json
